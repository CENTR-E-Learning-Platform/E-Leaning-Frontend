export interface Session {
    id: string;
    price: number;
    title: string;
    startTime: string;
    durationMinutes: number;
    grade: number;
    reminder: string;
    description: string;
}